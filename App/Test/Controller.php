<?php declare(strict_types=1);

namespace App\Test;



use Pebble\Template;
use Pebble\DBInstance;


class Controller {
    public function index ($params) {

        
        /*
        $db = DBInstance::get();
        $rows = $db->getAll('task', []);
        foreach($rows as $row) {
            $update['end_date'] = $row['begin_date'];
            $db->update('task', $update, ['id' => $row['id']] );
        }
        
        */
        
    }
}